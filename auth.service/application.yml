server:
  port: 8080
spring:
  application:
    name: auth.service
  datasource:
    url: jdbc:postgresql://postgresdb:5432/auth_service
    username: postgres
    password: best_password
  kafka:
    consumer:
      group-id: auth.service
